{
  "name": "@cronos-mcp/server-sdk",
  "version": "0.1.0",
  "description": "MCP Server SDK with x402 payment middleware for Cronos",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["cronos", "x402", "mcp", "server", "sdk"],
  "license": "MIT",
  "dependencies": {
    "@cronos-mcp/core": "*",
    "@cronos-mcp/payment": "*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}